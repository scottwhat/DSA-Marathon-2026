# count paths

## alvin steps 
## main and helper functions off the ba

## helper function 
## takes in grid, r, c, memo

## check in memo
## check if row out of bounds r = len(grid) or c = len(grid[0])

## return 1 if hit the target (bottom right) r == len(grid) -1 and c == len(grid[0]) -1

## store return value in memo, can you use tuples in oython as keys? 

##
