# merge sort


def mergeSort(arr, s, e):
       #basic recursive merge sort
        # passing the array lets it be sorted in place
    if e - s + 1 <= 1: 
        return arr
    
    #if its not, 
    recursive calls to reduice down the arrays 
    #  get a middle index 

    #merge the result of the sub problems, only using temporary arrays here 

    # parent funciton recursion
    # 

    #child merge function  

    #### STABLE SORTING
    # add elements from left first if they are equal to right. so if left <= right add left first
